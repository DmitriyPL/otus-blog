console.log("This is slider - only fo this page");

function slider() {
    const carouselEl = document.querySelector('.carousel-pl__inner');
    const carouselItemsEl = carouselEl.querySelectorAll('.carousel-pl__item');
    // carouselItemsEl.forEach(element => {
    // // accordions.push(new Accordion(element))
    // // })
    // // let isExpanded = false
    // // document.getElementById('toggle-all').addEventListener('click', () => {
    // // accordions.forEach(accordion => {
    // //     isExpanded ? accordion.collapse() : accordion.expand()
    // // })
    // // isExpanded = !isExpanded
    // // })
}

slider()